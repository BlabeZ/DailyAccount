# 智能记账助手

> 基于 Qt 6 / C++17 的桌面端个人记账应用，集预算管理、智能分类、储蓄目标于一体。

---

## 一、项目概述

**智能记账助手**是一款 Windows 桌面记账工具，采用 Qt 6.9.3 框架和 C++17 标准开发。以"从记到管"为设计理念，不仅提供基础的流水记录和统计分析功能，还内置了**预算管理**、**智能分类引擎**和**储蓄目标追踪**三大智能模块，帮助用户从被动记账走向主动财务管理。

### 核心亮点

| 特性 | 说明 |
|------|------|
| 🧠 智能分类 | 从历史记录中学习关键词→分类映射，输入备注自动建议分类 |
| 📊 预算管理 | 按月/按分类设定预算，实时进度条 + 超支预警 |
| 🎯 储蓄目标 | 设定目标金额和截止日期，自动计算每月需存额度 |
| 📈 可视化统计 | 手绘饼图、柱状图、月度趋势，无需第三方图表库 |
| 🎨 现代 UI | 全局 QSS 样式表，侧边栏滑动菜单，卡片式布局 |

---

## 二、功能概览

### 🏠 概览页（Dashboard）
- **汇总卡片**：本月收入 / 支出 / 结余，HTML 富文本卡片
- **预算进度条**：总预算消耗进度，绿(<80%) → 橙(80-100%) → 红(>100%)
- **最近流水**：最近 10 条记录表格
- **支出分布**：按一级分类的横向进度条
- **储蓄目标卡片**：目标进度 + 每月/每天需存金额提示

### 📋 账目页（Flow）
- 按日期分组的树形列表，每日显示收入/支出汇总
- 筛选工具栏：日期范围 + 类型 + 分类级联菜单
- 添加 / 编辑 / 删除流水记录
- 编辑弹窗集成智能分类建议

### 📊 统计页（Statistics）
- 支出 / 收入饼图（QPainter 手绘，支持环形中空）
- 月度收支柱状图（绿=收入，红=支出）
- 时间范围筛选：本月 / 近3个月 / 近1年 / 全部
- 汇总数据卡片

### 💰 预算管理页（Budget）
- 月份选择器，支持前后 3 个月
- 总预算设置 + 分品类预算设置
- 每条预算实时显示进度条和已花费/剩余金额
- 超预算时状态栏红色警告

### 🎯 储蓄目标页（Goal）
- 创建目标：名称 + 金额 + 截止日期
- 自动计算：剩余月份 → 每月需存 → 每天需存
- 进度百分比 + 达成状态标识
- 支持删除和查看详情

### 🏷 分类管理页（Category）
- 预设分类（不可删）+ 自定义分类（可增删）
- 使用中分类保护（有流水记录的分类不可删除）
- **关键词映射管理**：查看/添加/删除/一键学习关键词→分类规则

### 🔧 其他功能页（Other）
- 数据导出：导出全部记录为 TXT 文件到桌面
- 清除数据：三重确认后清空所有数据

---

## 三、技术架构

```
记账/
├── backend/                    # 数据层（无 Qt GUI 依赖）
│   ├── record.h                #   核心结构体 Record
│   ├── storage.h / .cpp        #   文件读写（管道符分隔）
│   ├── category.h / .cpp       #   分类管理器
│   ├── ledger.h / .cpp         #   账本：CRUD + 统计
│   ├── budget.h / .cpp         #   预算管理器
│   ├── classifier.h / .cpp     #   智能分类引擎
│   └── goal.h / .cpp           #   储蓄目标管理器
├── gui/                        # 界面层（Qt Widgets）
│   ├── main_gui.cpp            #   入口：QApplication + QSS + 组件装配
│   ├── mainwindow.h / .cpp     #   主窗口：侧边栏 + QStackedWidget
│   ├── homepage.h / .cpp       #   入场首页
│   ├── dashboardpage.h / .cpp  #   概览页
│   ├── flowpage.h / .cpp       #   账目明细页
│   ├── flowdialog.h / .cpp     #   添加/编辑弹窗
│   ├── statisticspage.h / .cpp #   统计页（自绘图表）
│   ├── categorypage.h / .cpp   #   分类管理页
│   ├── otherpage.h / .cpp      #   其他功能页
│   └── *.ui                    #   Qt Designer 界面文件
├── data/                       # 运行时数据目录
│   ├── records.dat             #   流水记录
│   ├── categories.dat          #   自定义分类
│   ├── budgets.dat             #   预算数据
│   ├── keywords.dat            #   关键词映射
│   └── goals.dat               #   储蓄目标
├── jizhang.pro                 # Qt 项目文件
├── CLAUDE.md                   # AI 辅助开发指南
└── 记账.exe                    # 编译产物
```

### 数据流

```
StorageManager (文件读写)
    ↕
Ledger (CRUD + 聚合统计)
    ↕
MainWindow (持有所有 Manager 引用)
    ↕
各页面 (Dashboard / Flow / Statistics / Category / Budget / Goal / Other)
```

### 数据存储格式

所有数据以**管道符分隔**的纯文本文件存储于 `data/` 目录：

```
# records.dat
id|date|type|amount|category|note

# budgets.dat
2026-07|OVERALL|5000.00
2026-07|饮食|2000.00

# keywords.dat
瑞幸|饮食(咖啡)
地铁|交通(地铁)

# goals.dat
id|name|targetAmount|currentSaved|deadline|createdDate
```

---

## 四、构建与运行

### 环境要求

| 组件 | 版本 / 路径 |
|------|------------|
| Qt | 6.9.3 (MinGW 64-bit) — `D:/tools/Qt/6.9.3/mingw_64` |
| MinGW 工具链 | `D:/tools/mingw64/bin` |
| C++ 标准 | C++17 |
| 操作系统 | Windows |

### 编译命令

```bash
export PATH="D:/tools/Qt/6.9.3/mingw_64/bin:D:/tools/mingw64/bin:$PATH"
qmake jizhang.pro
mingw32-make -j$(nproc)

# 输出文件重命名
mv -f jizhang.exe "记账.exe"
```

### 运行

双击 `记账.exe` 即可启动，程序窗口默认最大化。

---

## 五、智能模块详解

### 5.1 智能分类引擎

**学习算法**：遍历所有历史记录，提取备注中的 2~3 字中文词组，统计每个词组与各分类的共现频率。若某词组在特定分类下出现占比 ≥ 60% 且出现次数 ≥ 2，则自动建立关键词→分类映射。

**使用方式**：
1. 在分类管理页点击「从历史记录学习」一键建立映射
2. 也可手动添加关键词映射
3. 添加记录时输入备注，自动匹配关键词并建议分类

### 5.2 预算管理

- 按月 + 按分类双重维度设定预算
- 每次刷新时从 Ledger 同步实际花费
- 进度条颜色自适应：绿色(安全) → 橙色(警告 ≥80%) → 红色(超支 ≥100%)
- 状态栏实时显示预算剩余或超支警告

### 5.3 储蓄目标

- 设定目标名称、金额和截止日期
- 根据当前总结余自动计算进度
- 智能计算：剩余月数 → 每月需存 → 每天需存
- 达成后显示 ✅ 标记

---

## 六、UI 设计规范

### 色彩体系

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色调 | `#3498DB` | 按钮、导航高亮、链接 |
| 收入/正向 | `#27AE60` | 收入金额、正结余、预算安全 |
| 支出/负向 | `#E74C3C` | 支出金额、负结余、超支警告 |
| 警告 | `#F39C12` / `#E67E22` | 预算接近上限 |
| 背景 | `#F5F7FA` | 页面主背景 |
| 卡片 | `#FFFFFF` | 卡片背景 |
| 深色文字 | `#2C3E50` | 标题、正文 |
| 浅色文字 | `#7F8C8D` / `#95A5A6` | 辅助说明 |

### 组件样式

- **导航按钮**：`QPushButton[class="nav"]`，活跃态 `#3498DB` 高亮
- **卡片**：`QFrame[class="card"]`，白色背景 + 10px 圆角 + 浅灰边框
- **进度条**：纯 QWidget 实现，无第三方依赖
- **图表**：QPainter 手绘饼图和柱状图，无 Qt Charts 依赖
- **侧边栏**：200px 固定宽度，一级/二级菜单滑动切换动画

---

## 七、版本历史

| 版本 | 主要变更 |
|------|---------|
| v1~v13 | 基础功能：CRUD、分类管理、统计图表、数据导出、UI 优化 |
| v14 | 新增三大智能模块：预算管理、智能分类、储蓄目标 |
| v15 | 修复页面错位，储蓄目标独立为功能页 |

---

## 八、许可证

本项目为个人学习作品，仅供学习参考使用。

---

> 如果你每天少喝一杯 ¥15 的奶茶，一年可多存 ¥5,475 —— 智能记账助手，从记到管，让每一分钱都有归属。
